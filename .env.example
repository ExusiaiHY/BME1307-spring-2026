# Optional Docker Compose overrides.
# Copy to `.env` only if your local paths differ from the repository layout.

BUSAT_HOST_DATA_DIR=./Breast-ultrasound-samples/Ultrasound Samples
PART1_HOST_DATA_DIR=./data
OUTPUTS_HOST_DIR=./outputs
CACHE_HOST_DIR=./.cache
